function runCycle(){
    //timeCycle();
    while (model.time < model.cycle){
        //randomizer(randomHex(), randomHeight(), randomWidth())
        console.log("too much")
    }
}

// function timeCycle(one){
//     model.time = model.time + one;
//     setInterval(timeCycle(1), model.cycle*1000);
//     console.log(model.time);
//     if (model.time < model.cycle){
//         model.time = 0;
//     }
// }